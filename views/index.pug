extends layout

block content
  h1= title
  p Welcome to USA Ganesh #{title}
  P Ganesh
  button(type="button", class="btn btn-success", onclick="doBump()") Press me
  div(id="bump") 
    i Bump the count
  script. 
    var count = 0;
    var bump = 1;
    function doBump(){
      count = count + bump;
      bump = bump + 1;
      document.getElementById("bump").innerHTML="Count "+count+" with bump "+bump;
    }
  a(href="jeep") Jeep Page 